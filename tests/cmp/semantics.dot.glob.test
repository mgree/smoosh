mkdir -p bar/inner
touch bar/foo
touch bar/inner/foo
cd bar/inner
echo .*/foo # should be ../foo and ./foo
cd ../../
rm -r bar

# bash, dash, yash all work
# smoosh, fish, zsh fail

# core problem: Sys.readdir hides the "." and ".." entries!